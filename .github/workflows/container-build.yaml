name: Build container image
env:
  REGISTRY: ${{ secrets.REGISTRY }}
  REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}
  PROJECT: ${{ secrets.PROJECT }}
  TAG: ${{ github.event.inputs.tag || github.ref_name }}
  TMPDIR: /home/runner/.local/tmp

on:
  push:
    tags:
      - '*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag'
        required: true

jobs:
  container-image:
    runs-on: ubuntu-latest
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
            swap-size-mb: 2048
            root-reserve-mb: 4096
            remove-dotnet: 'true'
            remove-android: 'true'
            remove-haskell: 'true'
            remove-codeql: 'true'
            remove-docker-images: 'true'
            build-mount-path: '/home/runner/.local'
      - name: Create tmp directory
        run: mkdir -p $TMPDIR
      - name: Check disks
        run: df -h
      - name: Test container download
        run: podman pull docker.io/library/python
      - name: Check disks 2
        run: df -h
      - uses: actions/checkout@v4
      - name: apt update
        run: sudo apt update
      - name: Install podman
        run: sudo apt --yes install podman
      - name: Install go-task
        run: |
          curl -L -O https://github.com/go-task/task/releases/download/v3.37.2/task_linux_amd64.deb
          sudo dpkg --install task_linux_amd64.deb
          rm --force task_linux_amd64.deb
      - name: Authenticate to container registry
        run: task container-image:auth
      - name: Check XDG_RUNTIME_DIR
        run: echo "XDG_RUNTIME_DIR:${XDG_RUNTIME_DIR}"
      - name: Check auth file
        run: cat ${XDG_RUNTIME_DIR}/containers/auth.json
      - name: Build container image
        run: task container-image:build
      - name: Upload container image
        run: task container-image:push
